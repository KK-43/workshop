{% extends 'base.html' %}
{% load staticfiles %}
{% block body %}

 <html><!-- Latest compiled and minified CSS -->

<!-- Latest compiled and minified JavaScript -->
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/js/bootstrap.min.js" integrity="sha384-0mSbJDEHialfmuBBQP6A4Qrprq5OVfW37PRR3j5ELqxss1yVqOtnepnHVP9aJ7xS" crossorigin="anonymous"></script>
<body position="center" background-image : url({%static"images/images.jpeg"});background-repeat:no-repeat;background-size:100%;height: 140px;display:block;padding:0 !important;margin:0;" >
   

        <div class="col-md-8 col-md-offset-2 well">
<table class="table table-hover table-bordered table-responsive">
    <thead>
    <tr>
        <th>
            Name
        </th>
        <th>
            Description
        </th>
        <th>
            Manufacturer
        </th>
        <th>
            Price
        </th>
        <th>more</th>
    </tr>
    </thead>
    <tbody>
    {% for item in object_list %}
        <tr>
            <td> {{ item.name }} </td>
            <td> {{ item.description }}</td>
            <td> {{ item.manufacturer }}</td>
            <td> {{ item.price }}</td>
         <td> <a href="{% url 'chocolate_info' item.id %}">Info</a> </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</div>


               <head background="pic.jpeg">
               <link rel="stylesheet" href="{%  static 'css/bootstrap.min.css' %}">
               <link rel="stylesheet" href="{%  static 'css/bootstrap-theme.min.css' %}">
               <script type="text/javascript" src="{% static 'js/jquery-1.12.1.min.js'%}"></script>
               <script type="text/javascript" src="{% static 'js/bootstrap.min.js'%}"></script>
               </head>
       {% if user.is_anonymous %}
       nothing here
       {% else %}
       <a class="clo-md-6-col-md-offset-5"style="padding-top: 10%">
        <a href="{% url 'add_chocolate'  %}">
           <button class="btn-btn-primary">add a chocolate</button></a></a>
           {% endif %}

       </div>


 

</body>


{% endblock body %}

</html>
